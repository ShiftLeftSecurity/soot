Predicates:

EnterMonitor(Immediate i) -> entermonitor i;
ExitMonitor(Immediate i) : exitmonitor i;
identity(Local l, IdentityRef r) : l := r;

Transformations:

transformation(Local x) {
	pred def: x = ??;
	pred use: ? = staticinvoke <?: ? ?(?)>(x);
	
    at def: (def && AX !E(true U use));
	transform def {}
}